# Diagrams — WMB_CHAT

```mermaid
flowchart LR
  A[Doanh nghiệp] --> V[Trò chuyện doanh nghiệp - sinh viên]
  V --> C[Current view/controller]
  C --> D[DoanChat relation và Chat trong Neon]
  C --> O[Current UI result/side effect]
```

Sơ đồ mô tả current behavior, không phải target architecture.


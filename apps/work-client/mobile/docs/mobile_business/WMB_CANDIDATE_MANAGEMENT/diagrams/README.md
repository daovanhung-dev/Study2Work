# Diagrams — WMB_CANDIDATE_MANAGEMENT

```mermaid
flowchart LR
  A[Doanh nghiệp] --> V[Ứng viên và lọc CV]
  V --> C[Current view/controller]
  C --> D[CV/UngVien/DoanChat/Chat qua Neon]
  C --> O[Current UI result/side effect]
```

Sơ đồ mô tả current behavior, không phải target architecture.


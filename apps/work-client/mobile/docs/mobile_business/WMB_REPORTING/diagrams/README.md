# Diagrams — WMB_REPORTING

```mermaid
flowchart LR
  A[Doanh nghiệp] --> V[Thống kê và báo cáo]
  V --> C[Current view/controller]
  C --> D[EmployeeReportUI dùng fl_chart/local data]
  C --> O[Current UI result/side effect]
```

Sơ đồ mô tả current behavior, không phải target architecture.


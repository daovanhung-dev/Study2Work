<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Lịch phỏng vấn - Learn2Earn</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"/>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
     /* Cho phép TẤT CẢ thẻ <a> trong header hiển thị con trỏ dạng chọn */
    .header a {
      cursor: pointer;
    }
    /* Thêm cho dropdown item */
    .header .dropdown-item {
      cursor: pointer;
    }
    body {
      background: #f3f6fb;
      font-size: 0.95rem;
      font-family: 'Inter', sans-serif;
      margin: 0;
    }

    .card-soft {
      border-radius: 16px;
      border: 0;
      box-shadow: 0 4px 14px rgba(15, 23, 42, 0.08);
      background: #ffffff;
    }

    .small-muted {
      font-size: 0.85rem;
      color: #6b7280;
    }

    /* ================= HEADER ================= */
    .header {
      background: #ffffff;
      box-shadow: 0 4px 16px rgba(15, 23, 42, 0.06);
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .brand-logo {
        height: 60px;       
        width: auto;
        display: block;
        object-fit: contain;

        transform: scale(2.3);
        transform-origin: left center;
    }


    .navbar {
      min-height: 72px;
      display: flex;
      align-items: center;
      font-size: 0.9rem;
    }

    .navbar-brand img {
      height: 55px;
      object-fit: contain;
    }

    .navbar-nav .nav-link {
      font-weight: 500;
      color: #475569;
      padding: 8px 14px;
      border-radius: 999px;
      transition: 0.25s;
    }

    .navbar-nav .nav-link:hover {
      background: #EFF6FF !important;
      color: #2563EB !important;
    }

    .navbar-nav .nav-link.active {
      background: #DBEAFE !important;
      color: #1D4ED8 !important;
    }

    /* ICON THÔNG BÁO + CHAT */
    .nav-icon {
      width: 38px;
      height: 38px;
      margin-left: 8px;
      margin-right: 8px;
      border-radius: 50%;
      background: #f1f5f9;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #475569;
      font-size: 18px;
      transition: 0.25s;
    }

    .nav-icon:hover {
      background: #e2e8f0;
      color: #2563EB;
    }

    /* ===== DROPDOWN CV (chỉ hover mới xanh, không đậm sẵn) ===== */
    .l2e-dropdown:hover .dropdown-menu {
      display: block;
    }

    .dropdown-menu {
      display: none;
      background: #f8fbff;
      border-radius: 14px;
      border: 0;
      padding: 8px 0;
      box-shadow: 0 8px 20px rgba(0,0,0,0.08);
    }

    .dropdown-item {
      padding: 10px 18px;
      font-weight: 500;
      border-radius: 10px;
      color: #1e293b;
      transition: background 0.2s, color 0.2s;
    }

    .dropdown-item:hover {
      background: #e6edff !important;
      color: #1e6ee9 !important;
    }

    .dropdown-item.active,
    .dropdown-item:active {
      background: transparent !important;
      color: #1e293b !important;
    }

    .nav-link.dropdown-toggle {
      background: transparent !important;
      color: inherit !important;
    }

    /* ============ LỊCH PHỎNG VẤN ============ */
    .filter-pill {
      border-radius: 999px;
      padding: 5px 14px;
      border: 1px solid #e5e7eb;
      background: #fff;
      font-size: 0.8rem;
      cursor: pointer;
      transition: 0.2s;
    }
    .filter-pill.active {
      background: #1e6ee9;
      color: #fff;
      border-color: #1e6ee9;
    }

    .interview-item {
      border-radius: 12px;
      padding: 10px 12px;
      margin-bottom: 8px;
      border: 1px solid #e5e7eb;
      background: #ffffff;
      cursor: pointer;
      transition: background 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
    }
    .interview-item:hover {
      background: #eef2ff;
      border-color: #c7d2fe;
      box-shadow: 0 3px 10px rgba(15,23,42,0.08);
    }
    .interview-item.active {
      background: #e0edff;
      border-color: #93c5fd;
    }

    .status-badge {
      border-radius: 999px;
      padding: 3px 9px;
      font-size: 0.75rem;
      font-weight: 500;
    }
    .status-upcoming {
      background: #dcfce7;
      color: #166534;
    }
    .status-done {
      background: #e5e7eb;
      color: #374151;
    }
    .status-cancel {
      background: #fee2e2;
      color: #b91c1c;
    }

    .tag-pill {
      display: inline-flex;
      align-items: center;
      padding: 3px 8px;
      border-radius: 999px;
      background: #e5efff;
      color: #1e40af;
      font-size: 0.8rem;
      margin-right: 4px;
      margin-top: 2px;
    }

    /* ================= FOOTER ================= */
    .footer {
      background: #0F172A;
      color: white;
      padding: 56px 0 26px;
      margin-top: 70px;
    }
    .footer-title { font-size: 17px; font-weight: 600; margin-bottom: 18px; }
    .footer-links { list-style: none; padding: 0; }
    .footer-links a { color: #94A3B8; text-decoration: none; }
    .footer-links a:hover { color: white; }
    .footer-bottom {
      border-top: 1px solid #1E293B;
      padding-top: 18px;
      margin-top: 32px;
      text-align: center;
      color: #94A3B8;
      font-size: 0.85rem;
    }
  </style>
</head>
<body>

<!-- ================= HEADER ================= -->
<header class="header">
    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">

        <!-- Logo -->
        <a class="navbar-brand" role="button" data-route="/Student/Home" onclick="navigateTo('/Student/Home')">
          <img src="/img/logo-nobr.png" alt="Logo Learn2Earn" class="brand-logo">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
          aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
          <!-- MENU CHÍNH -->
          <ul class="navbar-nav mx-auto d-flex align-items-center">
            <li class="nav-item">
              <a class="nav-link" data-route="/Student/Home" onclick="navigateTo('/Student/Home')">Trang chủ</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" data-route="/Student/Profile" onclick="navigateTo('/Student/Profile')">Hồ sơ</a>
            </li>

            <!-- DROPDOWN CV -->
            <li class="nav-item dropdown l2e-dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="cvMenu">CV</a>
              <ul class="dropdown-menu" aria-labelledby="cvMenu">
                <li><a class="dropdown-item" data-route="/Student/CreateCV"
                    onclick="navigateTo('/Student/CreateCV')">Tạo CV</a></li>
                <li><a class="dropdown-item" data-route="/Student/UpdateCV"
                    onclick="navigateTo('/Student/UpdateCV')">Cập nhật CV</a></li>
                <li><a class="dropdown-item" data-route="/Student/TopCV"
                    onclick="navigateTo('/Student/TopCV')">TopCV</a></li>
              </ul>
            </li>

            <li class="nav-item">
              <a class="nav-link" data-route="/Student/Interview" onclick="navigateTo('/Student/Interview')">Lịch phỏng
                vấn</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" data-route="/Student/PostJobs" onclick="navigateTo('/Student/PostJobs')">Tin tuyển
                dụng</a>
            </li>
          </ul>

          <!-- ICON PHẢI -->
          <ul class="navbar-nav ms-auto d-flex align-items-center">
            <li class="nav-item">
              <a class="nav-icon" role="button" data-route="/Student/Noti" onclick="navigateTo('/Student/Noti')">
                <i class="bi bi-bell"></i>
              </a>
            </li>

            <li class="nav-item">
              <a class="nav-icon" role="button" data-route="/Student/Chat" onclick="navigateTo('/Student/Chat')">
                <i class="bi bi-chat-dots"></i>
              </a>
            </li>

            <li class="nav-item">
              <a class="nav-link" data-route="/Student/Setting" onclick="navigateTo('/Student/Setting')">Cài đặt</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <script>
      // chuyển trang với fade-out
      function navigateTo(url) {
        // nếu đang ở cùng trang thì không reload
        if (location.pathname === url || location.pathname.endsWith(url)) {
          return;
        }
        document.body.style.transition = 'opacity 0.25s';
        document.body.style.opacity = '0';
        setTimeout(() => { window.location.href = url; }, 220);
      }

      // tự động highlight menu theo current path (dùng data-route)
      function setActiveNavByPath() {
        const links = document.querySelectorAll('[data-route]');
        const path = location.pathname || '/';
        // mapping: nếu path là '/' thì treat as '/Student/Home'
        links.forEach(a => a.classList.remove('active'));
        // Try exact then contains
        let found = false;
        links.forEach(a => {
          const r = a.getAttribute('data-route');
          if (!r) return;
          if (path === r || path.endsWith(r) || path.includes(r)) {
            a.classList.add('active');
            found = true;
          }
        });
        if (!found) {
          // fallback: highlight Trang chủ if on base / or not matched
          const home = document.querySelector('[data-route="/Student/Home"]');
          if (home) home.classList.add('active');
        }
      }

      // close dropdown on small screens when collapsed by bootstrap automatically,
      // but ensure hover dropdown doesn't permanently display after window resize
      function resetDropdownsOnResize() {
        document.querySelectorAll('.dropdown-menu').forEach(dm => {
          if (window.innerWidth <= 992) {
            dm.style.display = '';
          }
        });
      }

      document.addEventListener('DOMContentLoaded', () => {
        setActiveNavByPath();
        window.addEventListener('popstate', setActiveNavByPath);
        window.addEventListener('resize', resetDropdownsOnResize);
      });
    </script>
  </header>


<!-- ================= CONTENT ================= -->
<div class="container py-4">

  <!-- TIÊU ĐỀ + FILTER NHANH -->
  <div class="card card-soft mb-4">
    <div class="card-body">
      <div class="d-flex flex-wrap justify-content-between align-items-center mb-3">
        <div>
          <h4 class="mb-0 fw-bold text-primary">Lịch phỏng vấn</h4>
          <div class="text-muted small">
            Theo dõi các lịch phỏng vấn sắp tới, đã diễn ra và trạng thái xác nhận với doanh nghiệp.
          </div>
        </div>
        <div class="text-end small-muted mt-2 mt-md-0">
          Sắp diễn ra: <span class="fw-semibold text-success" id="countUpcoming">0</span> cuộc
        </div>
      </div>

      <div class="d-flex flex-wrap gap-2">
        <button class="filter-pill active" data-filter="all">Tất cả</button>
        <button class="filter-pill" data-filter="upcoming">Sắp diễn ra</button>
        <button class="filter-pill" data-filter="done">Đã diễn ra</button>
        <button class="filter-pill" data-filter="cancel">Đã hủy</button>
      </div>
    </div>
  </div>

  <div class="row g-4">
    <!-- DANH SÁCH LỊCH PHỎNG VẤN -->
    <div class="col-lg-5">
      <div class="card card-soft">
        <div class="card-body">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <div class="fw-semibold">Danh sách lịch</div>
            <div class="small text-muted" id="countAll">0 lịch</div>
          </div>

          <div id="interviewList">

            <!-- ITEM 1 -->
            <div class="interview-item"
                 data-id="1"
                 data-status="upcoming"
                 data-company="ABC Software"
                 data-position="Thực tập sinh Java"
                 data-date="20/11/2025"
                 data-time="09:00 - 10:00"
                 data-location="Phòng họp online (Google Meet)"
                 data-mode="Online"
                 data-note="Bạn vui lòng vào phòng trước giờ bắt đầu 10 phút để test mic và camera."
                 data-contact="Chị Lan - HR (lan.hr@abcsoft.vn · 0988 123 456)">
              <div class="d-flex justify-content-between align-items-start">
                <div>
                  <div class="fw-semibold">ABC Software</div>
                  <div class="small-muted">Thực tập sinh Java</div>
                  <div class="small-muted">
                    <i class="bi bi-calendar-event me-1"></i>20/11/2025 · 09:00 - 10:00
                  </div>
                </div>
                <span class="status-badge status-upcoming">Sắp diễn ra</span>
              </div>
            </div>

            <!-- ITEM 2 -->
            <div class="interview-item"
                 data-id="2"
                 data-status="upcoming"
                 data-company="XYZ Tech"
                 data-position="Frontend Fresher"
                 data-date="22/11/2025"
                 data-time="14:00 - 15:00"
                 data-location="Văn phòng XYZ Tech, Quận Cầu Giấy, Hà Nội"
                 data-mode="Offline"
                 data-note="Mang theo CV bản in và bảng điểm (nếu có), đến sớm trước 15 phút."
                 data-contact="Anh Minh - HR (minh.hr@xyztech.vn · 0904 567 890)">
              <div class="d-flex justify-content-between align-items-start">
                <div>
                  <div class="fw-semibold">XYZ Tech</div>
                  <div class="small-muted">Frontend Fresher</div>
                  <div class="small-muted">
                    <i class="bi bi-calendar-event me-1"></i>22/11/2025 · 14:00 - 15:00
                  </div>
                </div>
                <span class="status-badge status-upcoming">Sắp diễn ra</span>
              </div>
            </div>

            <!-- ITEM 3 -->
            <div class="interview-item"
                 data-id="3"
                 data-status="done"
                 data-company="123 Labs"
                 data-position="Thực tập sinh Flutter"
                 data-date="10/11/2025"
                 data-time="08:30 - 09:15"
                 data-location="Online (Zoom)"
                 data-mode="Online"
                 data-note="Buổi phỏng vấn đã hoàn thành. Chờ kết quả từ doanh nghiệp."
                 data-contact="Chị Hương - HR (huong@123labs.vn)">
              <div class="d-flex justify-content-between align-items-start">
                <div>
                  <div class="fw-semibold">123 Labs</div>
                  <div class="small-muted">Thực tập sinh Flutter</div>
                  <div class="small-muted">
                    <i class="bi bi-calendar-event me-1"></i>10/11/2025 · 08:30 - 09:15
                  </div>
                </div>
                <span class="status-badge status-done">Đã diễn ra</span>
              </div>
            </div>

            <!-- ITEM 4 -->
            <div class="interview-item"
                 data-id="4"
                 data-status="cancel"
                 data-company="VNUA Career Center"
                 data-position="Buổi tư vấn CV & phỏng vấn thử"
                 data-date="05/11/2025"
                 data-time="15:00 - 16:00"
                 data-location="Phòng 204 nhà A1, VNUA"
                 data-mode="Offline"
                 data-note="Buổi phỏng vấn thử đã bị hủy theo đề nghị của sinh viên, có thể đăng ký lại sau."
                 data-contact="Trung tâm Hỗ trợ việc làm VNUA">
              <div class="d-flex justify-content-between align-items-start">
                <div>
                  <div class="fw-semibold">VNUA Career Center</div>
                  <div class="small-muted">Tư vấn CV & phỏng vấn thử</div>
                  <div class="small-muted">
                    <i class="bi bi-calendar-event me-1"></i>05/11/2025 · 15:00 - 16:00
                  </div>
                </div>
                <span class="status-badge status-cancel">Đã hủy</span>
              </div>
            </div>

          </div>
        </div>
      </div>
    </div>

    <!-- PANEL CHI TIẾT LỊCH -->
    <div class="col-lg-7">
      <div class="card card-soft mb-3">
        <div class="card-body">
          <div class="d-flex justify-content-between align-items-start mb-2">
            <div>
              <div class="fw-semibold" id="detailCompany">ABC Software</div>
              <div class="small-muted" id="detailPosition">Thực tập sinh Java</div>
            </div>
            <span class="status-badge status-upcoming" id="detailStatus">Sắp diễn ra</span>
          </div>

          <div class="small-muted mb-1">
            <i class="bi bi-calendar-week me-1"></i>
            <span id="detailDate">20/11/2025</span> ·
            <span id="detailTime">09:00 - 10:00</span>
          </div>
          <div class="small-muted mb-1">
            <i class="bi bi-geo-alt me-1"></i>
            <span id="detailLocation">Phòng họp online (Google Meet)</span>
          </div>
          <div class="small-muted mb-3">
            <i class="bi bi-camera-video me-1"></i>
            <span id="detailMode">Online</span>
          </div>

          <div class="mb-2 small">
            <span class="text-muted">Người liên hệ:</span>
            <span class="fw-semibold" id="detailContact">
              Chị Lan - HR (lan.hr@abcsoft.vn · 0988 123 456)
            </span>
          </div>

          <div class="mb-3">
            <div class="small text-muted mb-1">Ghi chú từ doanh nghiệp / nhà trường:</div>
            <div class="small" id="detailNote">
              Bạn vui lòng vào phòng trước giờ bắt đầu 10 phút để test mic và camera.
            </div>
          </div>

          <div class="mb-2">
            <span class="small text-muted">Gợi ý chuẩn bị:</span>
            <div class="mt-1">
              <span class="tag-pill">Kiểm tra CV</span>
              <span class="tag-pill">Ôn lại dự án</span>
              <span class="tag-pill">Chuẩn bị câu hỏi</span>
            </div>
          </div>
        </div>
      </div>

      <!-- HÀNH ĐỘNG -->
      <div class="card card-soft">
        <div class="card-body">
          <div class="fw-semibold mb-2">Hành động</div>
          <div class="d-flex flex-wrap gap-2 mb-2">
            <button class="btn btn-primary btn-sm" type="button" id="btnConfirm">
              <i class="bi bi-check2-circle me-1"></i> Xác nhận tham gia
            </button>
            <button class="btn btn-outline-primary btn-sm" type="button" id="btnRequestChange">
              <i class="bi bi-arrow-repeat me-1"></i> Xin đổi lịch
            </button>
            <button class="btn btn-outline-secondary btn-sm" type="button" id="btnJoinOnline">
              <i class="bi bi-camera-video me-1"></i> Mở phòng họp (demo)
            </button>
          </div>
          <hr>
          <div class="small text-muted mb-2">
            Ghi chú cá nhân (chỉ mình bạn thấy):
          </div>
          <textarea class="form-control form-control-sm mb-2" rows="2"
                    placeholder="Ví dụ: Chuẩn bị nói kỹ hơn về dự án Flutter, xem lại phần OOP, hỏi về lộ trình trở thành Fresher..."></textarea>
          <button class="btn btn-sm btn-outline-secondary" type="button">
            <i class="bi bi-save me-1"></i> Lưu ghi chú (demo)
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ================= FOOTER ================= -->
<footer class="footer">
  <div class="container position-relative">
    <div class="row">
      <div class="col-lg-4 mb-4">
        <h4 class="footer-title">Learn2Earn</h4>
        <p>Nền tảng tuyển dụng và tìm việc làm tại Việt Nam.</p>
      </div>

      <div class="col-lg-2 col-md-4 mb-4">
        <h4 class="footer-title">Về chúng tôi</h4>
        <ul class="footer-links">
          <li><a href="#">Giới thiệu</a></li>
          <li><a href="#">Tin tức</a></li>
          <li><a href="#">Tuyển dụng</a></li>
          <li><a href="#">Liên hệ</a></li>
        </ul>
      </div>

      <div class="col-lg-2 col-md-4 mb-4">
        <h4 class="footer-title">Ứng viên</h4>
        <ul class="footer-links">
          <li><a href="#">Tìm việc</a></li>
          <li><a href="#">Tạo CV</a></li>
          <li><a href="#">Hướng dẫn</a></li>
          <li><a href="#">Blog</a></li>
        </ul>
      </div>

      <div class="col-lg-2 col-md-4 mb-4">
        <h4 class="footer-title">Nhà tuyển dụng</h4>
        <ul class="footer-links">
          <li><a href="#">Đăng tin</a></li>
          <li><a href="#">Tìm ứng viên</a></li>
          <li><a href="#">Gói dịch vụ</a></li>
          <li><a href="#">Hỗ trợ</a></li>
        </ul>
      </div>

      <div class="col-lg-2 col-md-4 mb-4">
        <h4 class="footer-title">Liên hệ</h4>
        <ul class="footer-links">
          <li><a><i class="fas fa-phone me-2"></i>+84 123 456 789</a></li>
          <li><a><i class="fas fa-envelope me-2"></i>info@learn2earn.vn</a></li>
          <li><a><i class="fas fa-map-marker-alt me-2"></i>Hà Nội, Việt Nam</a></li>
        </ul>
      </div>
    </div>

    <div class="footer-bottom">
      © 2023 Learn2Earn. Tất cả quyền được bảo lưu.
    </div>
  </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  // Cập nhật panel chi tiết từ 1 item
  function setDetailFromItem(item) {
    const company  = item.getAttribute("data-company");
    const position = item.getAttribute("data-position");
    const date     = item.getAttribute("data-date");
    const time     = item.getAttribute("data-time");
    const location = item.getAttribute("data-location");
    const mode     = item.getAttribute("data-mode");
    const note     = item.getAttribute("data-note");
    const contact  = item.getAttribute("data-contact");
    const status   = item.getAttribute("data-status");

    document.getElementById("detailCompany").textContent  = company || "";
    document.getElementById("detailPosition").textContent = position || "";
    document.getElementById("detailDate").textContent     = date || "";
    document.getElementById("detailTime").textContent     = time || "";
    document.getElementById("detailLocation").textContent = location || "";
    document.getElementById("detailMode").textContent     = mode || "";
    document.getElementById("detailNote").textContent     = note || "";
    document.getElementById("detailContact").textContent  = contact || "";

    const statusBadge = document.getElementById("detailStatus");
    statusBadge.className = "status-badge"; // reset
    if (status === "upcoming") {
      statusBadge.textContent = "Sắp diễn ra";
      statusBadge.classList.add("status-upcoming");
    } else if (status === "done") {
      statusBadge.textContent = "Đã diễn ra";
      statusBadge.classList.add("status-done");
    } else if (status === "cancel") {
      statusBadge.textContent = "Đã hủy";
      statusBadge.classList.add("status-cancel");
    } else {
      statusBadge.textContent = "Khác";
    }
  }

  // Áp dụng filter theo trạng thái
  function applyInterviewFilter(filter) {
    const items = document.querySelectorAll(".interview-item");
    let upcomingCount = 0;
    let allCount = 0;

    items.forEach(item => {
      const status = item.getAttribute("data-status");
      allCount++;

      if (status === "upcoming") upcomingCount++;

      let show = true;
      if (filter === "upcoming" && status !== "upcoming") show = false;
      if (filter === "done" && status !== "done") show = false;
      if (filter === "cancel" && status !== "cancel") show = false;

      item.style.display = show ? "" : "none";
    });

    document.getElementById("countUpcoming").textContent = upcomingCount;
    document.getElementById("countAll").textContent = allCount + " lịch";
  }

  document.addEventListener("DOMContentLoaded", function () {
    const items = document.querySelectorAll(".interview-item");

    // Gắn click cho từng lịch phỏng vấn
    items.forEach(item => {
      item.addEventListener("click", function () {
        items.forEach(i => i.classList.remove("active"));
        item.classList.add("active");
        setDetailFromItem(item);
      });
    });

    // Set mặc định là item đầu tiên (nếu có)
    if (items[0]) {
      items[0].classList.add("active");
      setDetailFromItem(items[0]);
    }

    // Filter pills
    const filterButtons = document.querySelectorAll(".filter-pill");
    filterButtons.forEach(btn => {
      btn.addEventListener("click", function () {
        filterButtons.forEach(b => b.classList.remove("active"));
        btn.classList.add("active");
        const f = btn.getAttribute("data-filter");
        applyInterviewFilter(f === "all" ? "" : f);
      });
    });

    // Tính lại số lượng ban đầu
    applyInterviewFilter("");

    // Các nút hành động (demo)
    document.getElementById("btnConfirm").addEventListener("click", function () {
      alert("Demo: Xác nhận tham gia lịch phỏng vấn này (chưa kết nối backend).");
    });

    document.getElementById("btnRequestChange").addEventListener("click", function () {
      alert("Demo: Gửi yêu cầu xin đổi lịch phỏng vấn (chưa kết nối backend).");
    });

    document.getElementById("btnJoinOnline").addEventListener("click", function () {
      alert("Demo: Mở link phòng họp online nếu có (chưa gắn URL thật).");
    });
  });
</script>

</body>
</html>

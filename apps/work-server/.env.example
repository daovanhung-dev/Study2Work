# Legacy template only. The Work server does not load .env files or session state.
# Runtime configuration is defined in src/utils/constants.ts.

PORT=3000
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
DIRECT_DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"

JWT_SECRET="replace-with-a-local-jwt-secret"
JWT_EXPIRES="1d"

SUPABASE_URL="https://example.supabase.co"
SUPABASE_ANON_KEY="replace-with-a-local-supabase-anon-key"

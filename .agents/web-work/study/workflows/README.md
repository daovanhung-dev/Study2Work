# Study Web workflow

```text
web-work router
-> study/ subcontext
-> Vue entry/router/page/shared API source
-> Study API contract only when present
-> focused Vitest test
-> vue-tsc/build
-> update context + global worklog
-> context validator
```

Keep Vue component/state conventions local to this app. If a task introduces a
real Study API call, load `project/contracts.md`, record the missing/current
contract status and do not copy the Work Web API client shape without evidence.

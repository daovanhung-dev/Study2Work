# Web conventions placeholder

`CONTEXT_STATUS: SKELETON_ONLY`

Đọc đúng app `package.json`, TypeScript config, router, shared API code, feature
component và tests trước khi xác định convention. Không invent framework helper
hoặc abstraction dùng chung giữa hai web app.

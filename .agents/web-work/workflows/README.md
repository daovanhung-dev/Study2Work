# Web workflow placeholder

Task web phải chọn Vue hoặc React app, trace route -> page -> state/query -> API
contract -> UI states/tests. Bổ sung workflow source-backed, không dùng server
layer order cho frontend.

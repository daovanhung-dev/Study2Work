# Mobile conventions placeholder

`CONTEXT_STATUS: SKELETON_ONLY`

Đọc `analysis_options.yaml`, `pubspec.yaml`, source lân cận và test của đúng app
trước khi xác định Dart naming, state management, async/error handling, UI,
storage hoặc API convention.

# Mobile dependencies placeholder

`CONTEXT_STATUS: SKELETON_ONLY`

Chưa xác minh package/runtime/external-service dependency. Task cụ thể phải map
UI -> controller/state -> model/data helper -> external system bằng exact paths.

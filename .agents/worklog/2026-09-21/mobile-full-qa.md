# Mobile Student/Business full QA

- Date: 2026-09-21
- Scope: `mobile-work`
- Primary task type: `test`
- Secondary task types: `fix`, `docs`
- Status: `IN_PROGRESS` (paused for device unlock)

## PRIOR_WORKLOG_REVIEW

- Selector command: `deno run --allow-read scripts/select-worklogs.mjs --type test --limit 3`
- Selector result: `SHORTAGE`; 1 available test worklog selected, 2 worklogs missing from the requested limit.
- Reviewed: `.agents/worklog/2026-09-18/work-server-api-tests.md`
- Scope context reviewed: `.agents/context-map.md`, `.agents/mobile-work/AGENTS.md`, `.agents/mobile-work/INDEX.md`, `.agents/mobile-work/workflows/README.md`, `.agents/mobile-work/file-inventory.md`, `.agents/mobile-work/data-flow.md`, `.agents/project/design.md`, `.agents/worklog/TEMPLATE.md`

## Expected behavior

- Execute the user-provided full Android QA matrix for both `student` and `business` flavors on ADB device `12b304f9`.
- Produce screenshots and `report.md`, `results.json`, and `cleanup-summary.md` below `artifacts/mobile-qa/<timestamp>/`.
- Keep Student/Business side effects isolated to a unique `QA-MOBILE-<timestamp>` prefix and verify cleanup against baseline.

## Current behavior before this run

- Flutter SDK is available at `/home/daovanhung/development/Flutter/flutter`, but not on the default PATH.
- Physical device is connected: model `220333QPG`, Android 11, 720x1650, serial `12b304f9`.
- No `com.s2w.work.students` or `com.s2w.work.business` package was installed at preflight.
- Dependency resolution was blocked by `intl ^0.19.0` versus `go_router ^18.0.1`.
- Analyzer was blocked by late export directives in the two legacy chat controllers and missing mounted guards in the two settings screens.
- Initial debug build experiment in a temporary copy was blocked by `/tmp` being full; future Gradle temp files must use `/home/daovanhung/.cache/study2work-mobile-qa`.

## Changes in this run

- Raised mobile `intl` constraint to `^0.20.3`.
- Moved both chat polling export directives into the import/directive section.
- Added mounted guards after asynchronous logout in Student and Business settings.
- The remainder of this worklog will record analyze/test/build, device execution, screenshots, findings, and cleanup.

## Runtime checkpoint

- `flutter analyze`: pass, no issues.
- `flutter test`: pass, `7` passed and `2` intended Neon smoke tests skipped.
- Student/Business debug APK and debug AAB artifacts were generated by Gradle under `android/build/app/outputs`.
- Both application IDs were installed together once and verified with `pm path`.
- Student login screenshots captured through validation and wrong-credential states; Business/Student runtime continuation is paused because the physical device rebooted into a locked keyguard and Android blocks ADB input there.
- ADB/MIUI install confirmation also requires a physical tap for a newly installed Student package; this will be retried after the device is unlocked.

## Verification

Pending.

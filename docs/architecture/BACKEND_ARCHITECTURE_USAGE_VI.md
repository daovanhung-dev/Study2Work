# Backend Architecture Usage

This file is superseded by the canonical Server architecture document:

- `docs/architecture/SERVER_ARCHITECTURE.md`

Use `SERVER_ARCHITECTURE.md` for backend structure, API flow, persistence, migrations, async worker rules and verification commands.
